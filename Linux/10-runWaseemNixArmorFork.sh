# Get and run Waseem's Nix Armor code, for Linux hardening.
cd /root && git clone https://github.com/gerbsec/Nixarmor-But-Better.git && cd Nixarmor-But-Better && git checkout ccdc && sudo bash init.sh 
